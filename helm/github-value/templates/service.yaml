apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-value.fullname" . }}
  labels:
    {{- include "github-value.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "github-value.selectorLabels" . | nindent 4 }}
